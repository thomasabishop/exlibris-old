import React, { useState } from "react"
import DataGrid from "react-data-grid"
import axios from "axios"

const BookDataGrid: React.FC = () => {
   return <p>Data Grid</p>
}

export default BookDataGrid
